# 第5回の目標
- **ハンバーガーメニュー**の作り方を知る
- **positionでの配置**をできるようにする
- **クラス名を意識して**コーディングする

# コーポレートサイト
## 目的
企業や法人に関する情報を総括的に掲載するサイト。企業や法人のことを伝えたり、興味を持ってもらいお問い合わせしてもらうことなどが目的である。

## 基本構成
- トップページ（ホームページ）
- 詳細ページ
    - サービス
    - 事例
    - 会社概要
- ブログページ（運営しているサービスなどのページ）
- お問い合わせページ

※ 会社の伝えたいことによってページ構成は変動する。

# コーディングに入る前に：共通スタイル・共通パーツを見つける
コーポレートサイトのような複数ページ・複数セクションのサイトを作るときは、いきなり書き始めず、まず「共通化できるもの」を探すのがポイント。

## ① CSS変数（カスタムプロパティ）にまとめた方がいい値を見つける
色・コンテンツ幅・z-indexなど、サイト全体で繰り返し使う値は`:root`にまとめて管理する（教材ではスタイルガイドが参考になる）。
```CSS
:root {
  /* 色管理用の変数 */
  --black-color: #333;
  --white-color: #fff;
  --primary-color: #1863af;
  --accent-color: #fc7f24;
}

:root {
  /* コンテンツ幅管理用の変数 */
  --content-width-sm: 800px;
  --content-width: 960px;
  --content-width-lg: 1088px;
}

:root {
  /* z-index管理用の変数 */
  --z-index-back: -1;
  --z-index-default: 1;
  --z-index-header: 100;
  --z-index-menu: 150;
  --z-index-modal: 200;
}
```

## ② layout（レイアウト）でまとめられそうなスタイルを探す
セクションの上下の余白やコンテンツ幅など、ページをまたいで共通する「枠」に関わるスタイルをまとめる。
```CSS
.l_container-sm,
.l_container,
.l_container-lg {
  width: 100%;
  padding: 0 16px;
  margin: 0 auto;
}

.l_container-sm {
  max-width: calc(var(--content-width-sm) + 32px);
}

.l_container {
  max-width: calc(var(--content-width) + 32px);
}

.l_container-lg {
  max-width: calc(var(--content-width-lg) + 32px);
}

.l_contents {
  padding: 120px 0;
}
```

## ③ module（モジュール）でまとめられそうなスタイルを探す
ハンバーガーメニューやボタンなど、複数箇所で使い回すパーツ。最初から完璧に洗い出す必要はなく、作りながらまとめていけばよい。

これが第4回で学んだ「クラス名を意識してコーディングする」の実践にあたる。`l_`（レイアウト）・`m_`（モジュール）・ページ名（`top_`など）といった接頭辞を意識して命名していく。

# 演習-1：ヘッダーのコーディング
Point：ハンバーガーメニューを作れるようになる／positionでの配置／クラス名を意識してコーディングする

## 模範コード
```HTML
<header class="l_header">
  <h1 class="l_header-logo">
    <a href="index.html" class="l_header-logo_link">ZeroPlus</a>
  </h1>

  <button class="m_hamburger">
    <span class="m_hamburger-bar"></span>
    <span class="m_hamburger-bar"></span>
    <span class="m_hamburger-bar"></span>
  </button>
</header>
```
```CSS
.l_header {
  height: 72px;
  width: 100%;
  padding: 0 16px;
  display: flex;
  justify-content: space-between;
  align-items: center;
  position: absolute;
  top: 0;
  left: 0;
  z-index: var(--z-index-header);
}

.l_header-logo {
  height: 100%;
  font-size: 20px;
  font-weight: bold;
}

.l_header-logo_link {
  height: 100%;
  display: flex;
  align-items: center;
}
```
```CSS
.m_hamburger {
  width: 32px;
  height: 24px;
  position: relative;
  z-index: var(--z-index-menu);
}

.m_hamburger-bar {
  width: 100%;
  height: 2px;
  position: absolute;
  background: var(--black-color);
  left: 50%;
}

.m_hamburger-bar:first-child {
  top: 0;
  transform: translate(-50%, 0);
}

.m_hamburger-bar:nth-child(2) {
  top: 50%;
  transform: translate(-50%, -50%);
}

.m_hamburger-bar:last-child {
  top: 100%;
  transform: translate(-50%, -100%);
}
```
- `.l_header`：ページ全体で共通のレイアウトなので`l_`接頭辞
- `.m_hamburger`：他のページでも使い回すパーツなので`m_`接頭辞
- ヘッダーは`position: absolute;`でKV（ファーストビュー画像）の上に重ねて配置し、`z-index`はCSS変数（`--z-index-header`）で管理する
- `spanタグ`はインライン要素なので、`height`をつけるときは必ず`position: absolute;`＋`width/height`の指定と組み合わせる

# 演習-2：KVのコーディング
Point：positionで中央配置できるようにする／クラス名を意識してコーディングする

仕様：背景画像に薄く黒いフィルターがかかっている／影がかかっている文字がある／他でも使いまわされているボタンがある。

## 模範コード
```HTML
<div class="top_kv">
  <div class="top_kv-inner l_container-lg">
    <div class="top_kv_box">
      <p class="top_kv_copy">
        DIGITAL MARKETING<br />
        FOR START UP.
      </p>
      <p class="top_kv_sub-copy">
        スタートアップのためのデジタルマーケティングを支援
      </p>
      <div class="top_kv_btn-wrapper">
        <a href="" class="m_btn m_btn__mail">無料で相談する</a>
      </div>
      <!-- /.top_kv_btn-wrapper -->
    </div>
    <!-- /.top_kv_box -->
  </div>
  <!-- /.top_kv-inner -->
</div>
<!-- /.top_kv -->
```
```CSS
.top_kv {
  height: 100vh;
  background: linear-gradient(rgba(51, 51, 51, 0.2), rgba(51, 51, 51, 0.2)),
    url(../img/bg_kv.jpg) center top / cover;
}

.top_kv-inner {
  position: relative;
  height: 100%;
}

.top_kv_box {
  width: 100%;
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  text-align: center;
}

.top_kv_copy {
  font-size: 24px;
  font-weight: bold;
  color: var(--white-color);
  text-shadow: 2px 2px 2px rgba(170, 170, 170, 0.16);
}
```
```CSS
.m_btn {
  width: 100%;
  height: 100%;
  border-radius: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--white-color);
  font-weight: bold;
}

.m_btn__mail {
  background-color: var(--accent-color);
}

.m_btn__mail::after {
  content: "";
  width: 24px;
  height: 24px;
  background: url(../img/btn_email.svg) center / contain no-repeat;
  margin-left: 8px;
}
```
- 考え方：外側の箱（`.top_kv`＝画面いっぱいの背景）→内側の箱（`.top_kv_box`＝中央に置きたいテキストのかたまり）の順で組み立てる
- `.m_btn`は「ボタン」という役割のモジュールとして`m_`接頭辞をつけ、`.m_btn__mail`のようにモディファイアで見た目のバリエーションを表現している（第2回で学んだ複数クラスの考え方と同じ）

# 画像に色付きフィルターをかける
`linear-gradient()`を背景画像と重ねて指定することで、疑似要素を使わずに画像へ色フィルターをかけることができる。
```CSS
.top_kv {
  background: linear-gradient(rgba(51, 51, 51, 0.2), rgba(51, 51, 51, 0.2)),
    url(../img/bg_kv.jpg) center top / cover;
}
```
- `background`プロパティにはカンマ区切りで複数の背景（グラデーションや画像）を重ねて指定できる
- 先に書いたものほど手前に表示されるため、半透明の黒いグラデーションが画像の上に重なり、色フィルターのように見える

# 上下左右中央寄せ
ある要素を、親要素に対して縦横どちらも中央に配置する代表的な方法。
```CSS
.top_kv-inner {
  position: relative;
}

.top_kv_box {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
}
```
- `top: 50%; left: 50%;`だけでは、要素自身の左上の角が親要素の中央に来るだけで、要素全体は中央に来ない
- `transform: translate(-50%, -50%);`で、要素自身の幅・高さの半分だけ左上方向にずらすことで、要素の中心が親要素の中心と一致し、正確に上下左右中央寄せになる

### 応用：中央だけじゃない「基準点からのずらし方」
`transform: translate()`の値は、X軸・Y軸それぞれ独立して指定できる。「その軸を中央寄せしたいときだけ`-50%`」「基準点をそのまま端として使いたいときは`0%`」「反対側の端に揃えたいときは`-100%`」という考え方で組み合わせる。

例えば本回のハンバーガーメニューの3本線（`.m_hamburger-bar`）は、X軸は常に`left: 50%; transform: translateX(-50%);`で横方向だけ中央寄せしつつ、Y軸は`top`の値（`0%`/`50%`/`100%`）と`translateY`の値（`0`/`-50%`/`-100%`）を線ごとに変えることで、上端・中央・下端に配置し分けている。

| 揃えたい位置 | top（またはleft） | transform |
|---|---|---|
| 上端（or左端）に揃える | 0% | 0% |
| 中央に揃える | 50% | -50% |
| 下端（or右端）に揃える | 100% | -100% |

縦横どちらも中央にしたい場合は、この表のY軸・X軸をそれぞれ「中央に揃える」の組み合わせ（`top:50%; left:50%; transform: translate(-50%, -50%);`）にすればよい。片方の軸だけ中央寄せしたい場合は、もう片方の軸の`transform`を`0%`のままにしておく。

# 演習-3：PROBLEMのセクションの作成
見出しの左右に装飾ラインを入れる、疑似要素の応用テクニック。
```CSS
.m_section-title {
  position: relative;
}

.m_section-title::before,
.m_section-title::after {
  content: "";
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  height: 2px;
  width: 24px;
  background: var(--primary-color);
}

.m_section-title::before {
  left: 0;
}

.m_section-title::after {
  right: 0;
}
```
- `::before`と`::after`をカンマでまとめて共通スタイル（線の太さ・色など）を指定し、位置（`left`/`right`）だけを個別に上書きすることで、左右対称の装飾を効率よく作っている

# 演習-4：吹き出しを使ったデザイン
Point：positionでの配置

擬似要素（`::before`）を使って三角形を作り、吹き出しのように見せるテクニック。
```HTML
<div class="top_suggestion">
  <p class="top_suggestion_txt">
    ZeroPlusの経験豊富なスタッフが
    スタートアップの問題解決のお手伝いをします。
  </p>
</div>
<!-- /.top_suggestion -->
```
```CSS
.top_suggestion {
  padding: 120px 0;
  background: var(--primary-color);
  position: relative;
}

.top_suggestion::before {
  content: "";
  position: absolute;
  top: 100%;
  left: 50%;
  transform: translateX(-50%);
  border-width: 32px;
  border-style: solid;
  border-color: var(--primary-color) transparent transparent transparent;
}
```
- 三角形は、`width`と`height`を0にした要素に対して、4辺のボーダーの色を「表示したい方向だけ色をつけ、残りは`transparent`（透明）にする」ことで作られている
- `border-color: var(--primary-color) transparent transparent transparent;`（上だけ色がついている）ため、要素の下側に下向きの三角形が表示される
- `top: 100%;`でセクションのすぐ下に配置し、`left: 50%; transform: translateX(-50%);`で横方向中央に配置している

## 別パターン：テキストを囲む吹き出し
```CSS
.balloon_container {
  position: relative;
  display: inline-block;
  background: #ff9158;
  border-radius: 30px;
  padding: 16px;
  font-weight: bold;
  color: #fff;
}

.balloon_container::before {
  content: '';
  border-width: 10px;
  border-style: solid;
  border-color: #ff9158 transparent transparent transparent;
  position: absolute;
  top: 100%;
  left: 50%;
  transform: translateX(-50%);
}
```

# linear-gradient()（グラデーション）
背景を単色ではなく、2色以上の色を滑らかに変化させて表示できる関数。
```CSS
background: linear-gradient(方向, 色1, 色2, ...);
```
```CSS
.gradient_item {
  background: linear-gradient(to right, #ffbb1e, #ff9158);
}
```
- `to right`：左から右方向へグラデーションする（`to left`、`to bottom`や、`45deg`のように角度で指定することも可能）

# ハンバーガーメニュー（基本形）
３本線のアイコンのような形をしていて、クリックするとナビゲーションが開ける、慣例的な機能的デザインの一つ。
```HTML
<button class="hamburger">
  <span class="hamburger_bar"></span>
  <span class="hamburger_bar"></span>
  <span class="hamburger_bar"></span>
</button>
```
```CSS
.hamburger {
  width: 40px;
  height: 32px;
  position: relative;
}

.hamburger_bar {
  width: 100%;
  height: 4px;
  position: absolute;
  left: 0;
  background: #000;
}

.hamburger_bar:nth-child(1) {
  top: 0;
}

.hamburger_bar:nth-child(2) {
  top: 50%;
  transform: translateY(-50%);
}

.hamburger_bar:nth-child(3) {
  top: 100%;
  transform: translateY(-100%);
}
```
- `position`：これを使って配置することで、クリックした時の変形（アニメーション）をやりやすくしている
- `spanタグ`：インライン要素なので、`height`をつけるときは必ず`display: block;`（または`position: absolute;`）をかけること

# text-shadow
テキスト要素に対して、影をつけることができるCSSプロパティ。
```CSS
text-shadow: X軸方向の距離 Y軸方向の距離 ぼかしの大きさ 色;
```
```CSS
.txt-shadow_item {
  text-shadow: 8px 8px 3px #ccc;
}
```
- `box-shadow`は要素（箱）に対して影をつけるが、`text-shadow`は文字そのものに対して影をつける点が異なる

# background-positionとbackground-sizeプロパティ
`.top_kv`で使った`url(../img/bg_kv.jpg) center top / cover`は、実は複数のプロパティをまとめて書ける省略記法（ショートハンド）になっている。

```CSS
.top_kv {
  background-image: url(../img/bg_kv.jpg);
  background-position: center top;
  background-size: cover;
}
/* 上記3行をまとめて書いたものが↓ */
.top_kv {
  background: url(../img/bg_kv.jpg) center top / cover;
}
```

- `background-image`：表示する画像（や`linear-gradient()`）を指定する
- `background-position`：画像の表示位置を指定する（`center top`＝横中央・縦上）
- `background-size`：画像の大きさ・トリミング方法を指定する（`cover`＝要素を隙間なく覆うように拡大縮小）

`background-image`と`background-size`は別々の役割を持つプロパティなので、実際は同時に使うのが普通（`background-size`だけを単独で使っても、表示する画像がなければ意味がない点に注意）。

# 相対パスの考え方（フォルダをまたいだ参照）
CSSファイルの中から画像を参照するとき（`url(../img/bg_kv.jpg)`など）は、CSSファイル自身の場所を基準にした相対パスで書く。

```text
src/
├─ index.html
├─ css/
│   ├─ style.css   ← ここから見たパスを考える
│   └─ reset.css
└─ img/
    └─ logo.png
```

- `css`フォルダと`img`フォルダは、どちらも`src`フォルダの中にある「兄弟フォルダ」の関係
- `style.css`から見て、まず`../`で1つ上の階層（`src/`）に戻り、そこから`img/logo.png`と辿るので、正しいパスは`../img/logo.png`
- `img/logo.png`（`../`なし）だと「同じ階層に`img`フォルダがある」という意味になってしまい、`css`フォルダの中を探しにいってしまうので誤り
- `/img/logo.png`（先頭に`/`）は「サーバーの一番上の階層（ドメイン直下）からの絶対パス」という別の意味になる

# position:absoluteの基準要素について（補足）
`position: absolute;`をかけた要素は、「`position`に`relative`・`absolute`・`fixed`のいずれかが指定された、一番近い祖先要素」を基準に配置される。

- 本回のコーディングのように、基準にしたい要素にわざわざ`position: relative;`をかけるのは「狙った場所を基準にするため」であり、必須ではない
- もし祖先要素の誰にも`position`が指定されていない場合、`absolute`の要素は一番外側（`html`要素＝画面全体）を基準に配置される
- そのため、「`absolute`を使うときは必ず`relative`の指定が必要」という説明は誤りで、正しくは「狙った基準にしたいときは`relative`（など）を指定する」という理解が正確

# 本日のまとめ
1. 「ハンバーガーメニューの見た目を作る」手順がわかった
2. `linear-gradient`でフィルターをかけることができる
3. `position`での中央寄せの手順がわかった
4. CSS変数・`l_`/`m_`接頭辞など、クラス名を意識したコーディングの実践方法がわかった

# 参考記事
- [テキストに影をつけよう](https://zero-plus.io/media/l_html_css/text-shadow/)
- [positionとtransformで要素を上下左右中央寄せしよう](https://zero-plus.io/media/l_html_css/centered/)
- [linear-gradient()を使ってグラデーションしてみよう](https://zero-plus.io/media/l_html_css/linear-gradient/)
- [CSSで吹き出しを作ろう](https://zero-plus.io/media/l_html_css/speech-bubble/)
- [CSSでハンバーガーメニューアイコンを作ろう](https://zero-plus.io/media/l_html_css/hamburger-icon/)
- 中央寄せコツ：https://saruwakakun.com/html-css/basic/centering
