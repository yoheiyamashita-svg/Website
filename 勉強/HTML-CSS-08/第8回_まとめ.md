# 第8回の目標
- **レスポンシブを思い出す**
- **より実践的なレスポンシブに慣れる**

# レスポンシブの流れ（振り返り）
基本の流れは[[第3回]]で学んだ通り。
1. 今の見た目とデザインカンプを見比べる
2. 違いを見つける
3. コードに置き換えて書く

主な変更箇所もおさらいしておく：
- 横並びの箇所を縦並びにする（またはその逆）
- テキストの大きさ／改行を調節する
- 要素の横幅／高さを調節する
- 要素の表示／非表示を切り替える
- 余白の大きさを調節する

# 同じ詳細度で上書きする
レスポンシブでは、すでに指定されているスタイルをメディアクエリの中で上書きすることが多い。`:not(:first-child)`のような疑似クラス付きのセレクタを上書きしたいときは、必ずもともとと同じ書き方（同じ詳細度）で指定し直す必要がある。
```CSS
/* ベース */
.sample_item:not(:first-child) {
  margin-top: 24px;
}

/* 上書きするときも同じセレクタの形で書く */
@media screen and (min-width: 1080px) {
  .sample_item:not(:first-child) {
    margin-top: 0;
  }
}
```
詳細度が異なるセレクタで上書きしようとすると、意図通りに反映されないことがあるので注意する。

# ショートハンドの上書きに注意する
`background`や`margin`のようなショートハンド（省略記法）プロパティは、1つのプロパティに複数の値がまとまっている。メディアクエリ内で一部の値だけ変えたいつもりでも、ショートハンドごと上書きすると、指定していない他の値まで初期値に戻ってしまうことがある。
```CSS
/* ベース */
.sample {
  margin: 64px auto 0;
}

/* 上側の余白だけ変えたいのに、ショートハンドで書くと他の値も再指定が必要になる */
@media screen and (min-width: 1080px) {
  .sample {
    margin: 0 0 0; /* 本来残したい auto が消えてしまう例 */
  }
}
```
変えたい値だけを個別プロパティ（`margin-top`など）で指定し直す方が、意図しない上書きを避けやすい。

# calc()で横幅を均等に分割する
画面幅によって「1行に並べる数」を変えたいとき、`calc()`を使うと余白込みで均等に横幅を分割できる。
```CSS
/* 余白16pxを挟んで3列に均等分割する場合 */
.sample_item {
  width: calc((100% - 16px * 2) / 3);
}
```
- 余白の本数は「列数－1」になる点に注意する（3列なら間の余白は2本）
- `justify-content: space-between;`で並べる方法と、`calc()`で幅まで計算してしまい`gap`で並べる方法の、どちらでも実現できる

# フレキシブルレイアウトという考え方
デザインカンプには載っていない画面幅（スマホとタブレットの間など）でも、レイアウトが崩れずに綺麗に見えるようにコーディングする考え方。レイアウトの変え方自体に厳密な正解はなく、次のポイントを押さえていれば自由とされている。
- 変更に強く、保守性が高い
- 不必要に長くない
- フレキシブルレイアウトである（決まった画面幅以外でも崩れにくい）

📎 [デザインデータからコーディング！ブログサイトのレスポンシブ化（フレキシブルレイアウトの解説あり）](https://zero-plus.io/media/l_html_css/coding-practice-responsive/)

# コーディングを効率化するスニペット登録
エディタにメディアクエリのスニペット（`@lg`や`@sm`と打つと展開される定型コード）を登録しておくと、同じ記述を何度も打たずにコーディングを効率化できる。人によって登録した打ち方は異なるので、自分の環境でどう登録したか覚えておくとよい。

# 本日のまとめ
1. レスポンシブの基本の流れは、これまでの回と変わらない（デザインと見比べる→違いを見つける→CSSで書く）
2. メディアクエリ内で上書きするときは、元のセレクタと同じ詳細度で書く
3. ショートハンドプロパティは、一部だけ変えたいときに他の値まで上書きしてしまうことがある
4. `calc()`を使うと、余白を考慮した均等な横幅分割ができる
5. デザインカンプにない画面幅でも崩れない「フレキシブルレイアウト」を意識する

# 参考記事
- [様々なデバイスに対応するためにレスポンシブを学ぼう](https://zero-plus.io/media/l_html_css/responsive-web-design/)
- [デザインデータからコーディング！ブログサイトのレスポンシブ化](https://zero-plus.io/media/l_html_css/coding-practice-responsive/)
- [デザインデータからコーディング！お問合せフォームのレスポンシブ化](https://zero-plus.io/media/l_html_css/coding-practice-contact-responsive/)
- [flexboxを使いこなそう](https://zero-plus.io/media/html-css/flexbox/)
